# Jetson Knowledge

Data-first NVIDIA Jetson knowledge module for `device-knowledge`.

This package owns Jetson device profiles, documentation indexes, prompt
fragments, and research seeds so product hosts such as RDK Studio can consume
Jetson knowledge from the shared trusted artifact instead of maintaining a
local source copy.

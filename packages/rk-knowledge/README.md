# Rockchip RK Knowledge

Data-first Rockchip RK knowledge module for `device-knowledge`.

This package owns RK3588/RK3588S device profiles, documentation indexes, prompt
fragments, command patterns, failure hints, and research seeds so product hosts
such as RDK Studio can consume Rockchip knowledge from the shared trusted
artifact instead of treating RK boards as generic Linux only.

# D-Moss Adapter

D-Moss adapter placeholder.

This package will convert device knowledge modules into the D-Moss
`KnowledgeModule` contract so RDK Studio can consume external knowledge packages
without changing its user experience.

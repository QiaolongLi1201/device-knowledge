# Device Knowledge MCP Server

Standalone MCP server placeholder.

The first implementation should expose read-only device knowledge as MCP
resources, tools, and prompts. It must not access RDK Studio credentials, SSH
sessions, local devices, or product UI state.
